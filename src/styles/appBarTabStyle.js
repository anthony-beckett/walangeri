import { StyleSheet } from "react-native"
import theme from "../theme"

const appBarTabStyle = StyleSheet.create({
    text: {
        color: 'white',
        paddingHorizontal: 10,
        paddingVertical: 15,
        fontWeight: theme.fontWeights.bold,
    }
})

export default appBarTabStyle