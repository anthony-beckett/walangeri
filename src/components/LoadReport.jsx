/**
 * LoadReport Component
 *
 * Placeholder component for loading reports. This component currently renders an empty View.
 * Future implementations can include functionality to search or filter existing reports.
 */

import { View } from 'react-native';

const LoadReport = () => {
    return (
        <View></View>
    )
}

export default LoadReport